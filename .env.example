# inferencia — env example for Docker / Coolify
# Copy to .env and set values. Never commit .env.

# Required in container: listen on all interfaces
INFERENCIA_HOST=0.0.0.0
INFERENCIA_PORT=8080

# Ollama backend (for local Mac + Docker use host.docker.internal)
INFERENCIA_BACKEND_URL=http://host.docker.internal:11434

# API keys (comma-separated; required — no keys file in image)
INFERENCIA_API_KEYS=sk-your-secret-key

# Optional
# INFERENCIA_LOG_LEVEL=debug
# INFERENCIA_LOG_FORMAT=text
# INFERENCIA_RATELIMIT_RPS=10
# INFERENCIA_RATELIMIT_BURST=20
