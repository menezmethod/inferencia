# Kokoro TTS Server — Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0

# TTS engine
kokoro>=0.9.0,<1.0.0

# Audio I/O
soundfile>=0.12.1,<1.0.0
numpy>=1.24.0,<2.0.0

# Prometheus metrics
prometheus-client>=0.21.0,<1.0.0

# Pydantic (shipped with fastapi, explicit pin for safety)
pydantic>=2.0.0,<3.0.0
